# ── Data ────────────────────────────────────────────
data/raw/*
data/warehouse/*
!data/warehouse/.gitkeep
!data/raw/.gitkeep

# ── DuckDB ──────────────────────────────────────────
*.duckdb
*.duckdb.wal
*.db

# ── Streaming-training resume state (transient, regenerate locally) ──
_streaming_checkpoint.json

# ── dbt ─────────────────────────────────────────────
dbt_project/target/
dbt_project/logs/
dbt_project/dbt_packages/

# ── Python ──────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/

# ── Jupyter ─────────────────────────────────────────
.ipynb_checkpoints/
*/.ipynb_checkpoints/*

# ── ML Model Artifacts ─────────────────────────────
*.pkl
*.pt
*.joblib
!models/xgboost_model/.gitkeep
!models/lstm_model/.gitkeep
models/query_plan_finetune/adapter/*.safetensors
models/query_plan_finetune/gguf/*.gguf
models/query_plan_finetune/merged/

# ── IDE / OS ────────────────────────────────────────
.claude/
.cursor/
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
.env

# ── Local logs ─────────────────────────────────────
logs/

# ── Node / Frontend ────────────────────────────────
frontend/node_modules/
frontend/dist/
frontend-web/node_modules/
frontend-web/.next/
frontend-web/*.tsbuildinfo
frontend-web/dev.log

# ── Docker ──────────────────────────────────────────
.docker/
site

# ── Python caches / test artifacts ─────────────────────────────────
.pytest_cache/
.mypy_cache/
.cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
pip-wheel-metadata/

# ── dotenv variations (local overrides) ──────────────────────────────
.env.local
.env.*

# ── Node/npm logs ───────────────────────────────────────────────────
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── IDE / editor extras
*.iml
.ropeproject/


# CDK synth output -- build artifact, never source
infra/cdk/cdk.out/

# graphify extraction cache -- version-keyed build artifact, re-churns on every
# graphify upgrade. The graph itself (graph.json/html, GRAPH_REPORT.md) is tracked.
graphify-out/cache/
kaggle.json
