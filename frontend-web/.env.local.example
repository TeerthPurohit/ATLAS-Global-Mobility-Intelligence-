# Server-only: read by next.config.js's rewrites() to proxy /api/* to the
# FastAPI backend. Never prefix with NEXT_PUBLIC_ -- that would inline it
# into the client bundle, exposing the backend's real URL to the browser.
BACKEND_URL=http://localhost:8000
