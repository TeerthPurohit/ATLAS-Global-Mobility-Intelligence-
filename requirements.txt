# ── Data Processing ─────────────────────────────────
duckdb>=1.0,<2.0
pandas>=2.0,<3.0
numpy>=1.24,<2.0
pyarrow>=14.0,<16.0

# ── dbt ─────────────────────────────────────────────
dbt-core>=1.8,<2.0
dbt-duckdb>=1.8,<2.0

# ── ML / Forecasting ────────────────────────────────
scikit-learn>=1.4,<2.0
xgboost>=2.0,<3.0
torch>=2.1,<3.0
statsmodels>=0.14,<0.15

# ── Spatial ─────────────────────────────────────────
shapely>=2.0,<3.0
geohash2>=1.1,<2.0

# ── RAG / LLM ───────────────────────────────────────
openai>=1.0,<2.0
langchain>=0.3,<1.0
langchain-community>=0.3,<1.0
chromadb>=0.5,<1.0

# ── Backend ─────────────────────────────────────────
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0

# ── Testing / Dev ──────────────────────────────────
pytest>=8.0,<9.0
httpx>=0.27,<1.0
jupyter>=1.0,<2.0
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0
